import { Routes } from '@angular/router';
import { LoginComponent } from './login/login/login.component';

export const ComponentsLayoutRoutes: Routes = [{ path: 'login', component: LoginComponent }];
